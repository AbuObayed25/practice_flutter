import 'package:flutter/cupertino.dart';

class CameraFragment extends StatelessWidget {
  const CameraFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my CameraFragment"),
      ),
    );
  }
}
